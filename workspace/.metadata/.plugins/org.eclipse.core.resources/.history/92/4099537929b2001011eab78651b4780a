exports.create = function(args) {
	var ui = require_once('js/ui');
	
	var win = ui.window('Home Window');
	var placeholder = ui.label('showing fugitives',{color:'#fff'});
	
	win.addChild(placeholder);
	
	var tabs = ui.tabView([
		{
			name:'fugitives',
			on:'images/fugitives_on.png',
			off:'images/fugitives_off.png'
		},
		{
			name:'captured',
			on:'images/captured_on.png',
			off:'images/captured_off.png'
		}
	],{
		bottom:0
	});
	
	tabs.addEventListener('tabChange', function(e) {
		(e.idx === 0) ? placeholder.text = 'showing fugitives' : placeholder.text = 'showing captured';
	});
	
	return win;
};
